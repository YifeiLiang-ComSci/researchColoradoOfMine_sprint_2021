You also need to install alz and utilsforminds package from Minds GitLab. Clone the alz and utilsforminds repository, and then command 
'pipenv install -e "path/to/alz"',
and
'pipenv install -e "path/to/utilsforminds"'.

